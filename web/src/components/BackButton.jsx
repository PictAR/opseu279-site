// web/src/components/BackButton.jsx

// Back buttons are intentionally disabled site-wide.
// If we ever want them again, we can restore the previous implementation.
export default function BackButton() {
  return null;
}
